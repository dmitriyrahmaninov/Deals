import React from 'react';
import AddContainer from '../../components/AddContainer'

export default function Add({deals}) {
    return (
        <div>
            <AddContainer deals={deals}/>
        </div>
    )
}
